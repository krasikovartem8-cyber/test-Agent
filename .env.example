# Нужен хотя бы один ключ. Провайдер выбирается автоматически (Anthropic, если задан, иначе OpenAI),
# либо явно: LLM_PROVIDER=anthropic | openai
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
# LLM_PROVIDER=auto

# Anthropic: основная модель и модель DOM-субагента
AGENT_MODEL=claude-opus-5
SUBAGENT_MODEL=claude-sonnet-5

# OpenAI: основная модель и модель DOM-субагента
OPENAI_MODEL=gpt-5.4
OPENAI_SUBAGENT_MODEL=gpt-5.4-mini

# Глубина рассуждений: low | medium | high | xhigh | max
AGENT_EFFORT=high

# Максимум шагов за одну задачу до запроса подтверждения
MAX_STEPS=80
# Порог контекста (токенов), после которого история сжимается
CONTEXT_TOKEN_LIMIT=150000

# Браузер: chrome (установленный Google Chrome) или chromium (скачанный Playwright)
BROWSER_CHANNEL=chrome
# Каталог профиля браузера (сохраняет логины/куки между запусками)
USER_DATA_DIR=.browser-profile
# Запись видео браузера в ./recordings (1 = включить)
RECORD_VIDEO=0
